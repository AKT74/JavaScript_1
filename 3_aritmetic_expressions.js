/*
aritmetic expressions is a combination of
operands (valus=es, variables, etc.)
operator (+ - * / %)
that can be evaluated to a value
ex. y = x + 5;
*/

// let student = 20;

// student = student + 2;       tambah
// student = student - 2;       kurang
// student = student * 2;       kali
// student = student / 2;       bagi
// student = student % 3;       sisa (modulus)
// student = student ** 2;      pangkat
// student =2;                //shortcut tambah(berlaku ke semua yang di atas)

// console.log(student);

/*
operator precedence
1. parenthesis ()
2. exponents
3. multiplication & division
4. addition & subtraction
*/

let result = 1 + 2 * (3 + 4);

console.log(result)